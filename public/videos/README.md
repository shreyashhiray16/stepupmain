# Hero background video

The hero uses `public/videos/transaformwr.mp4` as its background video.

Replace that file with another MP4 to change the homepage background.

The hero video is muted, autoplaying, looping, and displayed behind the existing content with a light overlay for readability.